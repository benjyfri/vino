backbone_name: "dinov3_vits16plus"
backbone_type: "hf_auto"
pretrained_path: "facebook/dinov3-vits16plus-pretrain-lvd1689m"
freeze_mode: "frozen"
pooling: "auto"
normalize_lvd_imagenet: true

input_adapter:
  type: "conv1x1"

head:
  type: "binary"
  hidden_dim: 256
  dropout: 0.1
